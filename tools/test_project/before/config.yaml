application:
  name: User Management System
  version: 1.0.0
  environment: development

server:
  host: localhost
  port: 3000
  timeout: 30

database:
  type: postgresql
  host: localhost
  port: 5432
  name: userdb
  pool:
    min: 2
    max: 10

logging:
  level: info
  format: json
